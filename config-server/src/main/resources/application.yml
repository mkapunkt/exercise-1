server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mkapunkt/exercise-1
          search-paths: configuration-files
          try-master-branch: false
